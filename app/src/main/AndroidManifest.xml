<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outbound WebSocket to GoSosmed agenthub; no inbound ports needed. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.GosomedAgent"
        android:supportsRtl="true">

        <!-- Validation/control channel reachable from `adb shell am broadcast`
             (P0): lets the server-hub-less flow run dump/tap/text on whatever
             app is currently foreground. Exported so adb can deliver it. -->
        <receiver
            android:name=".AgentReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.gososmed.agent.CMD" />
            </intent-filter>
        </receiver>

        <!-- AccessibilityService: source of truth for the UI hierarchy and
             the executor for tap/text/swipe — no root, no adb required. -->
        <service
            android:name=".AgentAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
