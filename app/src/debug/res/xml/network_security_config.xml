<?xml version="1.0" encoding="utf-8"?>
<!-- Override khusus build DEBUG (resource merging src/debug menimpa src/main):
     cleartext dibuka untuk pengembangan LAN (ws://192.168.1.x:8080, emulator
     10.0.2.2, dst). Build RELEASE TETAP menolak cleartext penuh (lihat
     src/main/res/xml/network_security_config.xml). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
