.gradle/
build/
local.properties
*.iml
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/
/captures
/app/build/
# Downloaded APK artifacts (CI output), never committed
var/apk/
*.apk
# CI-downloaded keystore copy; the source of truth is keystore/gososmed-release.jks
var/ks/
# CI-downloaded artifact dir
var/

# P0-1 (Plan 07): signing material TIDAK di repo. Keystore lama MATI
# (pernah publik); keystore baru hanya di GitHub Secrets + backup offline
# pemilik (lihat CHANGELOG v0.6.0).
keystore/
*.jks
